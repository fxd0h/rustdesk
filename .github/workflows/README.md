# CI Workflows

This fork uses the upstream RustDesk CI workflows to build packages.

## Building packages

- **Manual trigger**: Go to Actions → "Full Flutter CI" → Run workflow
- **Tag trigger**: Push a tag like `v1.4.6-drm` to trigger `flutter-tag.yml`
